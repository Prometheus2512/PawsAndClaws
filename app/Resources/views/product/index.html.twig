{#
{% extends 'base.html.twig' %}

{% block body %}
    <h1>Products list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Price</th>
                <th>Description</th>
                <th>Category</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for product in products %}
            <tr>
                <td><a href="{{ path('product_show', { 'id': product.id }) }}">{{ product.id }}</a></td>
                <td>{{ product.name }}</td>
                <td>{{ product.price }}</td>
                <td>{{ product.description }}</td>
                <td>{{ product.category }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('product_show', { 'id': product.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('product_edit', { 'id': product.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('product_new') }}">Create a new product</a>
        </li>
    </ul>
{% endblock %}
#}

{% extends 'base.html.twig' %}

{% block content %}
    <div style="

    margin-top: 5%;
    margin-bottom: 5%;

">

        <!-- Adoption -->
        {#  <div class="container">
              <div class="row">
                  <div class="col-md-12 centered">
                      <h3><span>The adoption centre</span></h3>
                       </div>
              </div>
          </div>#}
        <!-- Adoption -->
        <div class="container">
            <div class="row">
                <div class="col-md-12 centered">
                    <h3><span>Find a gift that suits your pet</span></h3>
                </div>
            </div>
            <div class="row adoption">
                {% for product in products %}

                    <div class="col-md-4">
                        <a href="{{ path('product_show', { 'id': product.id }) }}" title="{{ product.name }}"><img src="{{ asset('images/dog-4.png') }}" alt="Saint Bernard" /></a>
                        <div class="title">
                            <h5>
                                <span data-hover="{{ product.price }} DT">{{ product.name }}</span>
                            </h5>
                        </div>
                    </div>
                {% endfor %}
                {% if products is empty %}
                <div class="col-md-12 centered">
                    <h3><span>No products found</span></h3>
                </div>
                {% endif %}
                <a     class="col-md-12 centered btn btn-default btn-green" href="{{ path('product_new') }}">Add a new product </a>


            </div>
        </div>
        <!-- Adoption end -->


    </div>


    {#   <h1>Pets list</h1>

       <table>
           <thead>
               <tr>
                   <th>Id</th>
                   <th>Name</th>
                   <th>Gender</th>
                   <th>Age</th>
                   <th>Status</th>
                   <th>Type</th>
                   <th>Breed</th>
                   <th>Childfriendly</th>
                   <th>Inorout</th>
                   <th>Actions</th>
               </tr>
           </thead>
           <tbody>
           {% for pet in pets %}
               <tr>
                   <td><a href="{{ path('pet_show', { 'id': pet.id }) }}">{{ pet.id }}</a></td>
                   <td>{{ pet.name }}</td>
                   <td>{{ pet.gender }}</td>
                   <td>{{ pet.age }}</td>
                   <td>{{ pet.status }}</td>
                   <td>{{ pet.type }}</td>
                   <td>{{ pet.breed }}</td>
                   <td>{% if pet.childfriendly %}Yes{% else %}No{% endif %}</td>
                   <td>{% if pet.inOrOut %}Yes{% else %}No{% endif %}</td>
                   <td>
                       <ul>
                           <li>
                               <a href="{{ path('pet_show', { 'id': pet.id }) }}">show</a>
                           </li>
                           <li>
                               <a href="{{ path('pet_edit', { 'id': pet.id }) }}">edit</a>
                           </li>
                       </ul>
                   </td>
               </tr>
           {% endfor %}
           </tbody>
       </table>

       <ul>
           <li>
               <a href="{{ path('pet_new') }}">Create a new pet</a>
           </li>
       </ul>#}
{% endblock %}
